'use strict';

const File   = require(appRoot.concat('/models/files'));
const moment = require('moment');
const fs     = require('fs');

module.exports = {
    save: save,
};

function save(req, res) {

}
